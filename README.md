# Digital Twin Chatbot — Local demo

Open `index.html` in a browser. Upload a `style_profile_*.md` file (the hackathon prompt format works). The app will extract the name and quote pool, generate a 10-item round mixing real and AI-paraphrased statements, and let you guess Real vs AI.

- Final score is saved to local history (ranking).
- Clear history with the button.
